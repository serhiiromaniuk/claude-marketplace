# <Phase N — Title>  |  <phase-N_short-kebab>

> Static once written. The contract for this task. If scope changes, write a new
> task — don't rewrite this.

## What
One sentence. What this task delivers.

## Why
Context. Why now, and which golden rule / phase gate it serves.

## Scope
**In:** what this task covers.
**Out:** what is explicitly excluded (and which task, if any, owns it).

## Done when
- [ ] Condition 1 — verifiable, not assumed (name the check)
- [ ] Condition 2
- [ ] (gate phases) the phase gate passes — see RULES.md / AGENTS.md §6

## References
- Governing spec: `<path/to/spec.md>`
  <!-- MACHINE-READ: loop/where.sh takes the FIRST backticked `*.md` path on the
       line beginning "- Governing spec:" as this task's governing document, and
       reports whether it is still a stub (a `**STUB` / `TODO(spec)` marker in its
       first 12 lines). Keep exactly one such line. Omit the line entirely if the
       project has no spec layer. -->
- RULES.md (golden rules, phase table) · `<other docs §...>`
- Related files: `src/...`
