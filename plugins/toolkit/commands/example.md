---
description: Example slash command — delete or replace with a real one.
---

# Example command

This is a placeholder slash command. When installed it is invoked as `/toolkit:example`.

Replace the body with the instructions Claude should follow when this command runs.
Arguments passed after the command are available as `$ARGUMENTS`.
